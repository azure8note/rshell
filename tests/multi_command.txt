cat makefile && cp makefile makefileCpy && ls
echo testing || uname || hostname
mkdir TESTING && ls -a 
mv TESTING TESTED
ls -l
ps;pwd
rm -f makefileCpy
ls
rm -r TESTED; mkdir FINAL; ls
rmdir FINAL ; ls
uname
exit
